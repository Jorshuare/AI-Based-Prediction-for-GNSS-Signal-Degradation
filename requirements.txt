# SENTINEL-GNSS requirements
# Install: pip install -r requirements.txt
#
# Install PyTorch FIRST (before this file), choosing the right CUDA version:
#   CUDA 12.4+ (driver >= 550):  pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
#   CUDA 11.8   (driver >= 452):  pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
#   CPU-only:                     pip install torch torchvision torchaudio
#
# Check your driver version with: nvidia-smi

# --- Deep learning ---
torch>=2.2.0
torchvision>=0.17.0
torchaudio>=2.2.0

# --- Data & ML ---
numpy>=1.26.0
pandas>=2.2.0
scikit-learn>=1.4.0
imbalanced-learn>=0.12.0
scipy>=1.13.0

# --- Visualisation ---
matplotlib>=3.8.0
seaborn>=0.13.0

# --- Utilities ---
tqdm>=4.66.0
joblib>=1.4.0
