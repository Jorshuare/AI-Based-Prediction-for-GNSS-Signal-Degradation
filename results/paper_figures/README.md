# Publication Figures — Index

Generated by `python -m src.utils.make_paper_figures`. Each figure is **PDF** (vector, LaTeX)
+ **PNG** (300 dpi, slides). **Style:** cividis colour-blind-safe palette (single source of
truth `PALETTE`/`civ()`), single-source `FONTS` (14 pt bold legends/labels), **no titles**
(captions live in the paper), composite figures carry **(a)/(b)/(c)** panel labels. All
numbers traceable to `papers/RESULTS_REFERENCE.md` / the results JSONs.

| File | Shows | Use in |
|------|-------|--------|
| `fig01_multihorizon` | Macro-F1 + MCC at +5/+15/+30 s with 95% CI | Paper A §5.1 |
| `fig02_perclass_f1` | Per-class F1 across horizons | Paper A §5.2 |
| `fig03_model_comparison` | All methods' +5s Macro-F1 (in-domain) | Paper A §5.4 / Paper B §3 |
| `fig04_ablation` | Full vs LSTM-only vs Transformer-only | Paper A §5.3 |
| `fig05_crosscity_degraded` | DL vs RF per-class on Tokyo (RF DEGRADED collapse) | Paper A §5.5 / Conf. |
| `fig06_crosscity_gap` | In-domain vs Tokyo gap | Conference / Paper A |
| `fig07_ekf_rmse` | EKF RMSE (overall + blockage) by strategy | Paper B §4.4 |
| `fig08_ekf_trajectory` | True vs GNSS vs fixed vs adaptive EKF | Paper B §4.4 |
| `fig09_degraded_progress` | DEGRADED F1 across runs (2.6× gain) | Paper A / slides |
| `fig10_dataset_composition` | Epoch counts per source/city | Paper A §4 |
| `fig11_class_distribution` | Class balance across train/val/test | Paper A §4 |
| `fig12_latency` | DL vs RF inference latency (~9.5×) | Paper A §5.6 / Paper B |
| `fig13_reactive_vs_proactive` | Concept: proactive vs reactive | Paper A §1 / slides |
| `fig14_architecture` | Model block diagram | Paper A §3.4 |
| `fig15_pipeline` | Raw → features → model → EKF pipeline | Paper A §3 / Paper B |
| **`fig16_ensemble`** | **Ensemble (DL+XGB) in-domain vs cross-city — headline ensemble win** | **Paper B §3.3, Paper A §5.5** |
| **`fig17_persistence`** | **Persistence baseline vs models across horizons (is memory needed?)** | **Paper A/B baseline, slides** |
| **`figC1_main_results`** | Composite (a) multi-horizon (b) per-class (c) ablation | Paper A main figure |
| **`figC2_crosscity`** | Composite (a) Tokyo per-class (b) gap | Paper A §5.5 / Conference |
| **`figC3_ekf`** | Composite (a) trajectory (b) RMSE | Paper B §4.4 |
| **`fig21_urbannav_filter_comparison`** | Blocked-segment RMSE per filter (UrbanNav Tokyo) | Paper B §4 |
| **`fig22_urbannav_severity_sweep`** | When adaptive-R helps (multipath severity crossover) | Paper B §4 |
| **`fig23_inference_comparison`** | (a) model/ensemble cross-city quality (b) fusion RMSE — the full inference stack | Paper A/B, docs §6 |

> Generate the EKF/inference figures with `make_ekf_urbannav_figures` and
> `make_inference_comparison`. Evaluation figures from `evaluate.py` (confusion matrices, ROC, PR, calibration, attention
> heatmaps, feature saliency, lead-time histogram) live in `../figures/` and complement these.
>
> **Key narrative (Run 15):** in-domain trees lead; **cross-city, the DL+XGBoost soft-vote
> ensemble is best** (Macro-F1 0.892, DEGRADED 0.896). RandomForest collapses cross-city
> (DEGRADED 0.148); **XGBoost transfers** (0.784). Persistence baseline scores 0.91 at +5 s —
> the models' value is catching transitions + longer horizons.
